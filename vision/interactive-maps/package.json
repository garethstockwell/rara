{
  "name": "test-npm",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run open",
    "open": "concurrently \"http-server -a localhost -p 1234\" \"open http://localhost:1234\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "http-server": "^14.1.1"
  },
  "dependencies": {
    "maplibre-gl": "^5.7.3"
  },

  "source": "src/map/heritage_trail.js",
  "targets": {
    "module": {
      "includeNodeModules": true,
      "context": "browser",
      "outputFormat": "esmodule"
    }
  }
}
